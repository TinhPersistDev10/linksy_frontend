name: Frontend CI

on:
  push:
    branches: [main, master, dev, develop]
  pull_request:
    branches: [main, master, dev, develop]

env:
  NODE_VERSION: 20

jobs:
  build:
    name: Install, lint, and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_STUN_URLS: ${{ vars.NEXT_PUBLIC_STUN_URLS }}
          NEXT_PUBLIC_TURN_URL: ${{ secrets.NEXT_PUBLIC_TURN_URL }}
          NEXT_PUBLIC_TURN_URLS: ${{ secrets.NEXT_PUBLIC_TURN_URLS }}
          NEXT_PUBLIC_TURN_USERNAME: ${{ secrets.NEXT_PUBLIC_TURN_USERNAME }}
          NEXT_PUBLIC_TURN_CREDENTIAL: ${{ secrets.NEXT_PUBLIC_TURN_CREDENTIAL }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linksy-frontend-next-build
          path: .next
          retention-days: 7
